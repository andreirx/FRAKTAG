{
  "name": "fraktag",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace=@fraktag/engine",
    "dev": "npm run dev --workspace=@fraktag/engine",
    "test": "npm run test --workspace=@fraktag/engine"
  }
}
